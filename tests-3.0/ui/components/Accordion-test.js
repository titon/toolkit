import React from 'react';
import { shallow } from 'enzyme';
import Accordion from '../../../js-3.0/ui/components/Accordion';

describe('ui/components/Accordion', () => {
    describe('render()', () => {
        const wrapper = shallow(<Accordion />);
    });
});
